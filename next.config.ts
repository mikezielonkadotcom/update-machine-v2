import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // Allow the [slug]/[filename] catch route to work
  // No special config needed — App Router handles this natively
};

export default nextConfig;
